Day 24 - Advance Terraform Modules and CI/CD Pipeline Scaling

Ansible - Deploy Docker Container with Kubernetes Using Helm


```yaml

- name: Deploy Docker Container with Helm
  hosts: Localhost
  tasks:
    - name: Install Helm
      command: curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
    - name: Deploy microservices with helm
      command: helm install own-app ./microservices-chart

Terraform - Multi-Module Infra with Outputs


module "own_vpc"{
    source = "./vpc"
}

module "own_instance"{
    source = "./instance"
    vpc_id = module.own_vpc.vpc_id
}

module "instance_id"{
    value = module.own_instance.instance_id
}

Kubernetes - Helm based Auto Scaling Deployment

apiVersion: apps/v1
kind: Deployment
metadata:
  name: own-web-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: own-web-app
  template:
    metadata:
      labels:
        app: own-web-app
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: own-web-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: own-web-app
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80


Jenkinsfile — Scaling CI/CD Pipeline

pipeline {
    agent any
    stages {
        stage('Scale Up') {
            steps {
                echo 'Scaling up Jenkins pipeline agents for heavy workloads...'
                // Simulating scaling logic
            }
        }
        stage('Build') {
            steps {
                echo 'Building project...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying project...'
            }
        }
    }
}


Docker
# Healthcheck

FROM nginx
HEALTHCHECK CMD curl --fail http://localhost || exit 1